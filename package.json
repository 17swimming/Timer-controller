{
  "name": "timer-controller",
  "version": "1.0.1",
  "description": "A personal time management tool",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-packager . Timer-Controller --platform=win32 --arch=x64 --out=dist --overwrite --icon=icon.ico",
    "packager": "electron-packager . Timer-Controller --platform=win32 --arch=x64 --out=dist --overwrite --icon=icon.ico"
  },
  "keywords": [
    "time",
    "management",
    "tracker",
    "electron"
  ],
  "author": "Timer Controller Team",
  "license": "MIT",
  "devDependencies": {
    "electron": "^26.0.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "auto-launch": "^5.0.6",
    "lowdb": "^1.0.0"
  },
  "build": {
    "appId": "com.timer.controller",
    "productName": "Timer Controller",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "portable"
    },
    "portable": {
      "artifactName": "${productName}-portable-${version}.${ext}"
    }
  }
}
